# Codex Web Bridge

> Private standalone extraction in progress. The validated integrated baseline currently lives in `lxxlx2/universal-web-api`; standalone development is performed on a separate branch until release acceptance is complete.

Codex Web Bridge is an unofficial local bridge for routing Codex Desktop/CLI model requests through ChatGPT Web while preserving local client-side tool execution.

The first standalone release is not published yet. Do not treat the default branch as a release artifact until the release-candidate gate is marked complete.

中文文档将在 standalone development branch 中维护，并在首次 release candidate 合并前与英文版同步。
